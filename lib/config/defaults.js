module.exports = {
    // network
    HOST:    window.location.hostname,
    PORT:    80,
    PREFIX:  '/kwe',
    // editor settings
    ASK_BEFORE_LEAVING: true,
    // RunJS params
    RUNJS_HOST: 'runjs.kevoree.org',
    RUNJS_PORT: 80,
    RUNJS_PATH: 'sharedGroup'
};