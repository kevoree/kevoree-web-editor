{{#hasLibraries}}
    <ul class="list-group libs-list">
        {{#libraries}}
            <li class="list-group-item lib-item" data-path="{{path}}">
                <input type="checkbox">
                <span class="badge">{{version}}</span>
                <span class="typedef-name"{{#type}} data-tdef="{{type}}"{{/type}}>{{name}}</span>
            </li>
        {{/libraries}}
    </ul>
{{/hasLibraries}}
{{^hasLibraries}}
    <div class="well">
        <p>No TypeDefinition found <em>(check your filters ?)</em></p>
    </div>
{{/hasLibraries}}